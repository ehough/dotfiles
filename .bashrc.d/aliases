# some more ls aliases
OS=`uname -s`

if [ "$OS" == "Darwin" ]
then
    alias ls='ls -alhG'
else
    alias ls='ls -alh --color=auto'
fi


# no vi please
if [ "$OS" == "Darwin" ]
then
    alias vi='mvim -v'
else
    alias vi=vim
fi


alias ps='ps --forest'
alias cp='cp -v'
alias rm='rm -v'